import SwiftUI

struct DateHeader: View { let date: Date; var body: some View { Text(formatDate(date)).font(.caption2).fontWeight(.medium).foregroundStyle(.white.opacity(0.6)).padding(.vertical, 4).padding(.horizontal, 12).background(Color.black.opacity(0.2)).clipShape(Capsule()).padding(.vertical, 4) }; private func formatDate(_ d: Date) -> String { let cal = Calendar.current; if cal.isDateInToday(d) { return Strings.today } else if cal.isDateInYesterday(d) { return Strings.yesterday } else { let f = DateFormatter(); f.dateStyle = .medium; f.timeStyle = .none; f.locale = Locale(identifier: "pl_PL"); return f.string(from: d) } } }
